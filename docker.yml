---
- name: Docker | install
  zypper:
    name: docker
    state: latest

- name: Docker | enable service
  systemd:
    name: docker
    state: started
    enabled: yes

- name: Docker | add user to group
  user:
    name: "{{ WORKSTATION_USER }}"
    groups: docker
    append: yes
